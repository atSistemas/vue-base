import HomeContainer from '../home.container.vue'
console.log(HomeContainer)
// Here are some Jasmine 2.0 tests, though you can
// use any test runner / assertion library combo you prefer
describe('HomeContainer', () => {
//   // Inspect the raw component options
//   it('has a created hook', () => {
//     expect(typeof HomeContainer.created).toBe('function')
//   })

//   // Evaluate the results of functions in
//   // the raw component options
//   it('sets the correct name', () => {
//     expect(typeof HomeContainer.name).toBe('string')
//     const name = HomeContainer.name()
//     expect(name).toBe('HomeMain')
//   })
})
